# tools/requirements_extended.txt
# Existing dependencies (already in repo)
anthropic>=0.30.0
pyyaml>=6.0.1

# ── Phase 1: Knowledge Base (Learn) ──
fastembed>=0.4.0               # Local embeddings — zero API cost, Windows compatible (no PyTorch)
chromadb>=0.4.24               # Local vector store for rule knowledge

# ── Phase 2: Code Generation ──
lxml>=5.1.0                    # XML generation and validation
python-dotenv>=1.0.0           # Auto-loads .env file for API key
