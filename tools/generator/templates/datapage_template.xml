<?xml version="1.0" encoding="UTF-8"?>
<!--
  PEGA Data Page Rule Template
  Jinja2 template — variables in {{ }} are replaced by xml_formatter.py
  Conforms to PEGA 8.x Data-Data-Page rule XML schema
-->
<rule-declare-pages
  pzInsKey="RULE-DECLARE-PAGES {{ pega_class }}!{{ rule_name | upper }}"
  pxObjClass="Rule-Declare-Pages"
  pyClassName="{{ pega_class }}"
  pyRuleName="{{ rule_name }}"
  pyLabel="{{ label | default(rule_name) }}"
  pyStructure="{{ structure | default('Page') }}"
  pyScope="{{ scope | default('Thread') }}"
  pyStorageType="{{ storage_type | default('MemorySerialized') }}"
  pyFlowStatus="Approved-Active"
>

  <!-- ═══════════════════════════════════════════════
       DATA SOURCES
       ═══════════════════════════════════════════════ -->
  <pageList name="pySources">
    {% for source in sources %}
    <pageNode>
      <pySourceType>{{ source.type }}</pySourceType>
      {% if source.type == "Connector" %}
      <pyConnectorClass>{{ source.connector_class | default('') }}</pyConnectorClass>
      <pyConnectorName>{{ source.connector_name | default('') }}</pyConnectorName>
      {% endif %}
      {% if source.type == "Activity" %}
      <pyActivityClass>{{ source.activity_class | default(pega_class) }}</pyActivityClass>
      <pyActivityName>{{ source.activity_name }}</pyActivityName>
      {% endif %}
      {% if source.type == "Report" %}
      <pyReportClass>{{ source.report_class | default(pega_class) }}</pyReportClass>
      <pyReportName>{{ source.report_name }}</pyReportName>
      {% endif %}
    </pageNode>
    {% endfor %}
  </pageList>

  <!-- ═══════════════════════════════════════════════
       PARAMETERS
       ═══════════════════════════════════════════════ -->
  <pageList name="pyParameters">
    {% for param in parameters | default([]) %}
    <pageNode>
      <pyParamName>{{ param.name }}</pyParamName>
      <pyParamType>{{ param.type | default('Text') }}</pyParamType>
      <pyRequired>{{ param.required | default('false') }}</pyRequired>
    </pageNode>
    {% endfor %}
  </pageList>

  <!-- ═══════════════════════════════════════════════
       REFRESH CONDITIONS
       ═══════════════════════════════════════════════ -->
  <pyRefreshStrategy>{{ refresh_strategy | default('WhenChanged') }}</pyRefreshStrategy>
  {% if refresh_timeout %}
  <pyRefreshInterval>{{ refresh_timeout }}</pyRefreshInterval>
  {% endif %}

  <pyDescription>{{ description | default('') }}</pyDescription>
  <pyDeveloperNote>Generated by PEGA AI Companion — review before import</pyDeveloperNote>

</rule-declare-pages>
